# Server
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# GitHub (optional - raises API rate limit from 60/hr to 5000/hr)
GITHUB_USERNAME=kunalvibhute
GITHUB_TOKEN=

# Contact form email (optional - if unset, messages are only saved to backend/data/messages.json)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=
